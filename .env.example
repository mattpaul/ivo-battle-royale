# Local defaults keep the app runnable without external API access.
BATTLE_ROYALE_AGENT_PROVIDER=mock

# Required when BATTLE_ROYALE_AGENT_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5
OPENAI_TEMPERATURE=0.2
OPENAI_MAX_OUTPUT_TOKENS=2048
